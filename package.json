{
  "name": "flashflow-ai",
  "private": true,
  "scripts": {
    "postinstall": "npm install --include=dev --prefix frontend && npm install --include=dev --prefix backend",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm start --prefix backend"
  }
}
